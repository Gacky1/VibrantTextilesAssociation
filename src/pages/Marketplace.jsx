import { useEffect, useState } from 'react'; import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'; import { faMagnifyingGlass,faSliders } from '@fortawesome/free-solid-svg-icons'; import Navbar from '../components/Navbar'; import Footer from '../components/Footer'; import { getMarketplaceCategories,getMarketplaceProducts } from '../data/marketplaceDatabase'; import { EmptyState,ErrorState,LoadingSkeleton,ProductCard } from '../components/marketplace/MarketplaceUI';
export default function Marketplace(){const [products,setProducts]=useState([]),[categories,setCategories]=useState([]),[search,setSearch]=useState(''),[category,setCategory]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState(''); const load=async()=>{setLoading(true);const [p,c]=await Promise.all([getMarketplaceProducts({search,category}),getMarketplaceCategories()]);setProducts(p.data||[]);setCategories(c.data||[]);setError(p.error);setLoading(false)};useEffect(()=>{const timer=setTimeout(load,250);return()=>clearTimeout(timer)},[search,category]);return <><Navbar/><main><section className="bg-vibrant-main px-6 pb-20 pt-36 text-white"><div className="section-container"><p className="mb-4 text-sm font-bold uppercase tracking-[.25em] text-rose-200">Verified B2B suppliers</p><h1 className="max-w-4xl text-4xl font-black tracking-tight md:text-6xl">Source India’s exceptional textiles with confidence.</h1><p className="mt-5 max-w-2xl text-lg text-white/80">Discover verified manufacturers, handloom collectives, exporters and specialist textile services. Connect directly through enquiries and RFQs.</p><div className="glass-panel mt-10 flex max-w-4xl flex-col gap-3 rounded-2xl p-3 text-slate-900 md:flex-row"><label className="flex flex-1 items-center gap-3 px-3"><FontAwesomeIcon icon={faMagnifyingGlass}/><input className="w-full bg-transparent py-3 outline-none" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search products, materials, techniques…"/></label><label className="flex items-center gap-2 rounded-xl bg-white px-4"><FontAwesomeIcon icon={faSliders}/><select className="bg-transparent py-3 outline-none" value={category} onChange={e=>setCategory(e.target.value)}><option value="">All categories</option>{categories.map(c=><option key={c.id} value={c.slug}>{c.name}</option>)}</select></label></div></div></section><section className="section-container py-16"><div className="mb-8 flex items-end justify-between"><div><h2 className="text-3xl font-black">Marketplace products</h2><p className="mt-2 text-slate-500">{products.length} verified listings</p></div></div>{loading?<LoadingSkeleton/>:error?<ErrorState message={error} retry={load}/>:products.length?<div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<EmptyState/>}</section></main><Footer/></>}
