import { useState } from 'react';
import { supabase } from '../lib/supabase';
const initial={accountType:'industry_member',email:'',password:'',fullName:'',phone:'',companyName:'',city:'',state:'',organizationName:'',organizationType:'Manufacturer',verificationStatus:'verified'};
export default function AccountCreateModal({onClose,onCreated}){const [form,setForm]=useState(initial),[busy,setBusy]=useState(false),[error,setError]=useState(''),[created,setCreated]=useState(null);const input='mt-1 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-primary-400';const field=(label,key,type='text')=><label className="text-xs font-bold uppercase text-slate-500">{label}<input required={['email','password','fullName'].includes(key)} minLength={key==='password'?8:undefined} type={type} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className={input}/></label>;const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const {data,error:invokeError}=await supabase.functions.invoke('admin-create-account',{body:form});if(invokeError)throw invokeError;if(data?.error)throw new Error(data.error);setCreated(data.user);await onCreated();}catch(err){setError(err.message||'Unable to create account')}finally{setBusy(false)}};return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-4"><div data-lenis-prevent className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-white shadow-2xl"><div className="sticky top-0 z-10 flex justify-between border-b bg-white p-6"><div><p className="text-xs font-bold uppercase tracking-widest text-primary-600">Master Admin operation</p><h2 className="text-2xl font-black">Create account</h2></div><button onClick={onClose} className="text-2xl">×</button></div>{created?<div className="p-8 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-emerald-100 text-2xl text-emerald-700">✓</div><h3 className="mt-4 text-xl font-black">Account created</h3><p className="mt-2 text-slate-500">{created.email} can now use the appropriate sign-in portal.</p><button onClick={onClose} className="mt-6 rounded-xl bg-primary-600 px-6 py-3 font-bold text-white">Done</button></div>:<form onSubmit={submit} className="grid gap-4 p-6 sm:grid-cols-2"><label className="text-xs font-bold uppercase text-slate-500">Account type<select value={form.accountType} onChange={e=>setForm({...form,accountType:e.target.value})} className={input}><option value="industry_member">Industry Partner</option><option value="buyer">Buyer</option></select></label><label className="text-xs font-bold uppercase text-slate-500">Verification<select value={form.verificationStatus} onChange={e=>setForm({...form,verificationStatus:e.target.value})} className={input}><option value="verified">Verified</option><option value="under_review">Under review</option><option value="pending">Pending</option></select></label>{field('Full name *','fullName')}{field('Email *','email','email')}{field('Temporary password *','password','password')}{field('Phone','phone')}{form.accountType==='industry_member'?<>{field('Organization name *','organizationName')}{field('Organization type','organizationType')}</>:<label className="text-xs font-bold uppercase text-slate-500 sm:col-span-2">Company name<input value={form.companyName} onChange={e=>setForm({...form,companyName:e.target.value})} className={input}/></label>}{field('City','city')}{field('State','state')}{error&&<p className="sm:col-span-2 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<p className="sm:col-span-2 text-xs text-slate-500">The email is confirmed automatically. Share the temporary password securely.</p><div className="sm:col-span-2 flex justify-end gap-3 border-t pt-4"><button type="button" onClick={onClose} className="rounded-xl border px-5 py-3 font-bold">Cancel</button><button disabled={busy} className="rounded-xl bg-primary-600 px-6 py-3 font-bold text-white disabled:opacity-60">{busy?'Creating…':'Create account'}</button></div></form>}</div></div>}
